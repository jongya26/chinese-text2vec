[project]
name = "chinese-text2vec"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "sentence-transformers>=5.2.0",
    "fastapi",
    "uvicorn[standard]",
]
